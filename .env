# Frontend API Base URL - ALWAYS use relative paths to avoid mixed content errors
# The Vite dev server or backend will proxy these to the actual API
VITE_API_BASE_URL=/api

# For development: access via http://localhost:5173 or http://172.28.181.92:5173
# For production: backend serves both frontend and API on port 5001
